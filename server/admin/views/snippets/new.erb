<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:snippets, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:snippets, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:snippet) %></h2>
    <div class="inner">
      <% form_for :snippet, url(:snippets, :create), :class => :form do |f| %>
        <%= partial "snippets/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
